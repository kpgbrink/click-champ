import {User} from './User';
// Because we do not have code generation, you must manually
// import all the entities here and export them as an array
// for the sake of typeorm.

export default [
    User,
];
